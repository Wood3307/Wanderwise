# 本次热点与单次足迹接入验收

以下为此前各轮本地整合的历史验收，原基线为 `fbc8d97`。最新 GitHub 星空与陆地合并的来源、修复和验证见 [2026-09-15 合并记录](merge-worlds-20260915.md)。

- 构建通过（Node 24.14.1）。
- `test:personal`：76 项通过，包括真实 IndexedDB 事务失败、重试、幂等和旧数据兼容。
- `test:galaxy`：149 项通过；`test:content`：12 项通过。
- `test:galaxy-connection`：11 组通过，保留原旅程站点与视角返回。
- 星系源码测试运行器通过；样式检查通过（635 个选择器、6 组资源、16 个动画）。
- `eslint src/features/galaxy` 通过，接收器和热点改动的定向检查通过。
- 桌面与 390px 手机各 4 组真实浏览器收发流程通过：显式导出、提交后领取、日志/原文、新 SPA 会话、舍弃、重开领取与重复交付。
- 宿主真实 `/api/explore` 经官方 CLI/缓存/预算返回 HTTP 200、`source=zhihu-hot`、12 个热点问题。
- 补充了本地中文字体兜底，原有可用系统字体仍优先。

全仓库 `npm run lint` 仍有 14 项既有错误，位于未被本次修改的 PanelShell、通用 UI 组件与 FrameDiagnostics；没有为通过检查而放宽这些规则。此项不标为通过。

浏览器原生关页提示无法自定义为导出按钮。取消原生关闭后进入应用内导出选择；主动结束可以直接选择导出。原生确认离开不会被当成导出授权。

## 本轮：清除与当前内容无关的天体

- 第一层隐藏回答恒星，第二层仅显示所选问题当前页的回答恒星，第三层保留文章恒星和当前页的段落行星。
- 未显示页的行星、轨道和点击区域同步隐藏；全部内容继续通过原有分页访问。
- 两份星空源码同步修复，未改动联合版本的字体、颜色和样式文件。
- 主应用构建、137 项单元测试通过；联合应用构建、150 项星空测试通过。真实场景回归覆盖层级切换、问题切换、反向缩放、段落数量及隐藏轨道。
- 4 项现有浏览器回归通过；联合预览在 1440px 和 390px 下验证三层浏览、段落拖动、翻页和 F 键阅览，均无页面异常。
- 对比截图和报告：主工作区 `artifacts/content-bodies/`（隔离测试数据，无真实知乎 API 调用）。

## 本轮：移除文字后的黑影

- 删除问题、回答、段落标签和中心标题背后的深色渐变，以及大范围暗色文字阴影；保留原有微光、细微描边和动画时序。
- 仅同步修改两份 `galaxy.css`，黄色字体的宿主样式、字体文件、Markdown/LaTeX 和星尘渲染代码未改动。
- 两个应用构建通过；样式检查通过。两项现有星尘/滚轮过渡浏览器回归通过。
- 联合预览 1440px / 390px 三层浏览验证通过，无页面异常。前后对比确认文字、字号、颜色、行高、标签尺寸和黄色按钮的发光样式一致。
- 截图与对比报告：主工作区 `artifacts/text-shadow/`。

## 本轮：虫洞与循环联想探索

- 原星海的文字投影会避开虫洞光环与提示区域，保持已有天体位置。
- 三个纵深均可从左下虫洞进入光隧道；超立方体空间每轮包含 12 个关联方向，支持自由飞行、靠近展开、实际飞入和点击穿越。下一片星海仍能继续进入虫洞。
- 查询主题、已有星海状态和单次旅行语义正确衔接；选中方向的搜索结果只请求一次，取消和结束旅行会中止并忽略迟到请求。
- 主应用构建、157 项单元测试通过；联合应用构建、171 项星空测试通过；星空目录 ESLint 通过，751 个样式选择器、6 组资源、17 个动画检查通过。
- 主应用与联合预览各 7 项浏览器回归通过：三个深度往返、连续两次穿越、WASD/升降与真实飞入、失败/取消、手机触控、正常动态隧道等待、结束旅行取消请求。均未出现页面异常。
- 联合真实接口验证：`恋爱` 返回 12 个语义联想方向，选择 `友谊` 后通过知乎搜索返回 HTTP 200、10 个结果，并携带真实来源链接。报告在主工作区 `artifacts/wormhole/live-connection.json`。
- 几何使用实例化结构；12 个坐标的场景约 39 个绘制调用，并按帧耗时调节 DPR。功能验收使用 Chromium SwiftShader 软件渲染，未将软件渲染帧率表述为硬件 GPU 帧率。
- 操作和接口说明见 `docs/galaxy-wormhole-exploration.md`。浏览器测试源码在主工作区 `tests/wormhole.spec.ts`；联合验收设置 `WORMHOLE_ENTRY_PATH=/galaxy`，截图在主工作区 `artifacts/wormhole-host-regression/`。

## 2026-09-15：虫洞与墙面空间第二版

- 入口扩大为蓝色浓密漩涡，移除可见名称、悬停说明及空间内的常驻文案、返回/坐标/重置按钮。
- 入程为居中的蓝白光纤隧道，出程为紫色云壁隧道；两个方向都等待目标的实际首帧，再开始揭幕。进入阶段原星海一直隐藏。
- 联想空间为 XYZ 三轴交汇的冷色层叠结构，包含非纯黑星云背景。话题贴合实际墙面并随中心方块退入；49 块方块沿法线逐层后退，穿入墙后 8 个世界单位才触发新搜索。回头可以沿原话题墙面返回。
- 鼠标自动环顾，W 加速、S 制动、A/D 侧推，空格/Shift 调整俯仰；包含惯性、阻尼、墙面碰撞和手机图标触控。
- 主应用与联合应用构建通过；主应用 162 项单元测试、联合星空 176 项通过；星空 ESLint 和样式检查通过（686 个选择器、6 组资源、17 个动画）。
- 两个应用的 9 项浏览器场景均验证通过：无文案入口/三个深度往返、墙面与连续搜索、鼠标和惯性、手动进入深度、回头返回、失败/取消、逐帧揭幕、手机、结束旅行取消请求。墙面快速通过改为逐帧记录，避免测试轮询漏掉短暂的坍缩阶段。
- 逐帧检查确认隧道中心始终为 (0.5, 0.5)，进入时原星海始终隐藏，出现揭幕进度时对应场景已经就绪。未出现页面异常。
- 图片：主工作区 `artifacts/wormhole-v2/`、`artifacts/wormhole-v2-integration/` 和 `artifacts/wormhole-v2-host/`；手动穿入复验记录在 `artifacts/wormhole-v2-host-entry/`、`artifacts/wormhole-v2-main-entry/`。

## 2026-09-15：平行宇宙替换超立方体

- 移除超立方体、墙面坍缩、飞机加速度与旧飞行按键；使用鼠标拖动旋转、滚轮缩放与手机单指/双指操作。
- 12 个关联词与原话题返回入口对应 13 个虫洞。包含五种形态，固定三维坐标；仿宋标签保持水平，随虫洞投影移动并避让。
- 所有穿越共用蓝白光纤着色器与 3100ms 时长，保留真实首帧就绪控制；紫色隧道着色分支与样式已删除。
- 创世之柱背景使用官方 1920×1080 JPEG（640,496 字节），CSS 压暗用于阅读；界面底部保留发布方要求的完整可见署名及链接。
- 主应用与联合应用构建通过；157 / 171 项单元测试通过，星空 ESLint 与样式检查通过（683 个选择器、7 组资源、17 个动画）。
- 两版的 8 项浏览器场景均验证通过：三个纵深往返、投影拖动与旧按键无效、连续滚轮锁定关键词/再次联想、原话题返回、失败/取消、双向同款隧道与就绪揭幕、手机拖动和双指缩放、结束旅行中止请求。页面缩放比例保持 1，触控只缩放宇宙。
- 无页面异常；浏览器测试使用隔离数据。图片与记录在主工作区 `artifacts/parallel-universe-v3/`。素材出处见 `docs/galaxy-parallel-background.md`，当前行为见 `docs/galaxy-wormhole-exploration.md`。
